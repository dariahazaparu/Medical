@model Licenta2022.Models.Reteta

@{
    ViewBag.Title = "Details";
}

<h2>Rețeta</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DataEmiterii)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DataEmiterii)
        </dd>
        <dt>
            <label>Medicamente</label>
        </dt>

        <dd>
            @foreach (var item in Model.RetetaXMedicament)
            {
                @(item.Medicament.Denumire + " - " + item.Doza)
                <hr />

            }
        </dd>

    </dl>
    
</div>
<p>

    <div class="btn-group">
        <button><a href="/Programare/Details/@Model.Programare.Id">Vezi programarea</a></button>
    </div>

</p>
